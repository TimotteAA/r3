export const MODULE_BUILDER_REGISTER = "module-builder-register"

/**
 * 所有的环境类型
 */
export enum EnvironmentType {
  TEST = "test",
  PREVIEW = "preview",
  DEVELOPMENT = "development",
  PRODUCTION = "production"
}
